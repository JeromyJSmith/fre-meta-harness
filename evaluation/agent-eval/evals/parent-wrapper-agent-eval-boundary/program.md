# Parent wrapper program snapshot

Required checks for this fixture:

1. identify the strongest honest local agent-eval evidence level
2. prefer dry over fake smoke
3. treat Docker-backed smoke as the next stronger lane
4. require `OPENAI_API_KEY` before claiming that smoke executed
