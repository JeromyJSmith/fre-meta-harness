# Tests

Parent wrapper validator surfaces live here.

Primary validator:

- `validate_parent_wrapper_contract.py`

The validator now enforces the parent-native peer mesh, peer message, peer
lifecycle, observability event, observability ingest, policy decision, library
distribution, benchmark emission, peer mesh runtime, governed agent roles, hook
manifest, and bounded capability matrix surfaces using extensible
class-and-coverage invariants instead of fixed closed-world ID lists.
