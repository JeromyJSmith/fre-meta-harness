[project]
name = "fre-meta-harness"
version = "0.1.0"
description = "Parent-only inbox protocol, routing, and gatekeeping scaffold for fre-meta-harness."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0,<1.0.0",
  "uvicorn>=0.30.0,<1.0.0",
  "pyyaml>=6.0.1,<7.0.0",
  "jsonschema>=4.23.0,<5.0.0"
]

[tool.uv]
package = false
