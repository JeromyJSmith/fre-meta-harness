# Gap map and content-strength slice

## Selected slice

Land a governed parent-owned artifact family that records:

- exact runtime truth carried forward from the current parent kernel
- the InfraNodus path actually used for this slice
- strong content that is already trustworthy
- weak content that still needs better source material, schemas, probes, or proof
- missing workflow maps and underspecified contract surfaces
- a runnable tool shortlist for the next strengthening pass

## Truth boundary

- Active parent runtime lanes stay bounded to `peer_mesh_local.same_host` and
  `inbox_protocol.same_host`.
- Cross-device peer mesh and inbox protocol remain blocked with exact
  dependencies.
- Intake ETL, research harvest, semantic cartography, and wrapper synthesis
  remain defined parent surfaces, not activated subsystem runners.
- Live InfraNodus stays blocked unless authenticated MCP proof is captured.

## Strong content

1. Governance kernel: inbox packet, routing, delegation, and triad topology are
   explicit and validated.
2. Same-host runtime truth: tool-health artifacts preserve commands, evidence,
   and blocked cross-device boundaries.
3. Compiled research baseline: capability, feature, gap, and source-index
   packets align to the governed manifest.
4. Subsystem architecture truth: the subsystem topology tranche exists on disk
   and preserves non-activation truth.

## Weak content

1. Live InfraNodus access is still blocked; only the bounded local substitute is
   proven.
2. Tool packaging remains note-backed, not contract-backed.
3. Subsystem runners do not yet have bounded activation probes or subsystem
   observability bundles.
4. Consume-folder source quality is mixed; transcript-heavy claims still need
   narrower packets and stronger terminal or frame evidence.

## Missing workflow maps

1. Workflow edge map from consume inputs through subsystem stages to compiled
   outputs.
2. Contract-to-runner map that states which activation probes exist and which
   lanes remain non-activated.
3. Evidence-strength map that ties source class to promotion boundaries.

## Runnable tool shortlist

- `gitnexus` for bounded structure and gap scans
- `graphify` for bounded graph extraction
- `scripts/build-parent-infranodus-artifacts.py` for the strongest honest local
  InfraNodus substitute
- `gh copilot`, `codex`, and `pi` as locally available runners

## Next governed prompt

Recommended structured prompt path:

- `prompts/PROMPT-20260522-WORKFLOW-STRENGTHENING.yaml`

Recommended Markdown companion path:

- `prompts/PROMPT-20260522-WORKFLOW-STRENGTHENING.md`
