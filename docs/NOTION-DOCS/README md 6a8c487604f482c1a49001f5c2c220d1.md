# README.md

## examples/

Canonical examples that validate against schemas.

<aside>
📎

**Purpose**: Human-readable index for the `examples/` directory and its minimum required example set.

**Inputs**: `schemas/*`; Output Styles; golden path expectations.

**Outputs**: Canonical example artifacts used to validate the schemas and prove portability.

**Validation tie-in**: Golden tests (example snapshots), Contract validation (schema/example pairing), Replay tests (deterministic examples).

**Failure modes**: No examples → schemas become theory; drifting examples → false confidence.

**Update / upstream path**: Any schema change must update at least one example and be recorded in the run log.

</aside>

```yaml
page_contract:
  purpose: "Index and checklist for canonical examples."
  inputs: ["schemas/*", "Output Styles", "Golden path"]
  outputs: ["Required example checklist", "Example coverage expectations"]
  validation_tie_in:
    gates: ["Golden tests", "Contract validation", "Replay tests"]
    checks:
      - "Examples validate against their schemas"
  failure_modes:
    - "Schemas without executable proof"
    - "Example drift breaks trust"
  upstream:
    update_artifact: "Examples-index update artifact"
    proposal_path: "Update README + update examples + rerun validate"
```

### Minimum v1 examples to add

- action-log/actions.example.jsonl
- plans/[finishing-touches.plan.md](http://finishing-touches.plan.md)
- runs/[0001.md](http://0001.md)
- reports/report.example.html
- reports/scorecard.example.json