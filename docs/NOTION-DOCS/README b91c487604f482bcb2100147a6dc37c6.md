# README

# Schemas

This directory stores machine-readable schemas for META-HARNESS artifacts.

- `living-document-manifest.schema.json`: JSON Schema representation of the YAML front and bottom matter convention.
- `agent-role-contract.schema.json`: JSON Schema for bounded agent role contracts.