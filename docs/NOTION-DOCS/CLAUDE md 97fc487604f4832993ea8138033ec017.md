# CLAUDE.md

This file provides guidance to Claude Code when working in this repository.

<aside>
📎

**Purpose**: Primary **wake-up entrypoint** for Claude inside the Portable Harness Package; defines read order + hard rules.

**Inputs**: `docs/harness/*` nucleus docs; schemas/validation docs.

**Outputs**: Deterministic operator behavior; consistent navigation + validation expectations.

**Validation tie-in**: Contract validation (entrypoint present), Static checks (rule compliance), Replay tests (consistent runs).

**Failure modes**: If ambiguous, Claude invents workflow; if links drift, boot sequence breaks.

**Update / upstream path**: Changes require Update Artifact + fresh-export validation run log.

</aside>

```yaml
page_contract:
  purpose: "Claude wake-up entrypoint for this portable harness repo."
  inputs: ["docs/harness/*", "schemas/README.md", "runs/*"]
  outputs: ["Boot order + hard rules + minimal validation instructions"]
  validation_tie_in:
    gates: ["Contract validation", "Replay tests"]
    checks:
      - "Links resolve; hard rules are enforceable"
  failure_modes:
    - "Broken pointers to nucleus docs"
    - "Ambiguous rules -> workflow drift"
  upstream:
    update_artifact: "Entrypoint/rules update artifact"
    proposal_path: "Patch + rerun golden path to green"
```

## Start here

- Goal: notion-216
- Golden path: notion-217
- Memory: notion-218
- Sources (pins + provenance): notion-219

## Hard rules

- Follow the golden path before making structural changes.
- Record every run in `runs/`.
- Pin provenance for any new skills/templates in `docs/harness/sources.md`.

## How to validate (minimum)

- Run schema validation (see `schemas/README.md`).
- Run golden path end-to-end once and produce a run log.