# plan.schema.json

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "plan.schema.json",
  "title": "*.plan.md Schema (frontmatter + required sections + bottom config)",
  "type": "object",
  "additionalProperties": false,
  "required": ["frontmatter", "sections", "bottomMatter"],
  "properties": {
    "frontmatter": {
      "type": "object",
      "additionalProperties": true,
      "required": ["name", "status", "overview"],
      "properties": {
        "name": { "type": "string" },
        "status": { "type": "string" },
        "overview": { "type": "string" }
      }
    },
    "sections": {
      "type": "array",
      "items": { "type": "string" },
      "default": ["Inputs", "Acceptance criteria"]
    },
    "bottomMatter": {
      "type": "object",
      "additionalProperties": true,
      "required": ["run"],
      "properties": {
        "run": { "type": "object" }
      }
    }
  }
}
```