# README.md

## compiler/

Markdown-first spec compiler outputs.

<aside>
📎

**Purpose**: Defines the minimum compiler outputs that make completeness checkable (not subjective).

**Inputs**: `docs/harness/*`, `schemas/*`, `examples/*`.

**Outputs**: `compliance.md` and `missing.md` (or v1 placeholders with explicit rationale).

**Validation tie-in**: Static checks + Drift detection + Publish decision (cannot ship with unknown gaps).

**Failure modes**: Missing compiler outputs → hidden missing work; compliance becomes opinion.

**Update / upstream path**: New artifact types require compiler rule updates and new checks.

</aside>

```yaml
page_contract:
  purpose: "Compiler output contract and minimum required files."
  inputs: ["docs/harness/*", "schemas/*", "examples/*"]
  outputs: ["compiler/compliance.md", "compiler/missing.md"]
  validation_tie_in:
    gates: ["Static checks", "Drift detection", "Publish decision"]
    checks:
      - "Compiler outputs exist and are referenced from runs"
  failure_modes:
    - "Unknown missing artifacts"
    - "Unenforced standards"
  upstream:
    update_artifact: "Compiler-contract update artifact"
    proposal_path: "Patch compiler + rerun validate"
```

### Minimum v1 compiler outputs

- [compliance.md](http://compliance.md)
- [missing.md](http://missing.md)

The compiler answers: what’s missing, what’s stale, what broke, did golden path drift?