# README

# Tools

Tooling in this template is intentionally lightweight.

- `validate_schemas.py` validates schema files and JSON agent role contracts.

Extend this directory with project-specific validators for YAML manifest extraction, Vectorworks fixture inspection, IFC parsing, iModel ingestion checks, and regression reports.