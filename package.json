{
  "name": "fre-meta-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:app": "bun app/server.ts",
    "dev:service": "uv run python -m uvicorn service.main:app --reload --host 127.0.0.1 --port 8787",
    "scan:inbox": "uv run python scripts/watchers/inbox_router.py",
    "smoke:router": "uv run python scripts/watchers/inbox_router.py",
    "smoke:app": "uv run python -m unittest tests.test_runtime_surfaces.AppServerSmokeTests",
    "smoke:service": "uv run python -m unittest tests.test_runtime_surfaces.ServiceRouteTests",
    "test:runtime": "uv run python -m unittest discover -s tests -p 'test_*.py'",
    "test:validator": "uv run --isolated --with jsonschema --with pyyaml python tests/validate_parent_wrapper_contract.py",
    "test": "uv run python -m unittest discover -s tests -p 'test_*.py' && uv run --isolated --with jsonschema --with pyyaml python tests/validate_parent_wrapper_contract.py",
    "dev": "printf 'Run bun run dev:app and bun run dev:service in separate terminals.\\n'",
    "check:inbox": "uv run python scripts/watchers/inbox_router.py"
  }
}
